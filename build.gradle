group 'com.practice.bhargav'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

apply plugin: 'java'

dependencies {
    testCompile(
            'junit:junit:4.12'
    )
    compile project(':LinkedListDS')
    compile project(':TreeDS')
    compile project(':StackDS')
}
